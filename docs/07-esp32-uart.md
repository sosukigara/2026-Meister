# 機能: ESP32 ↔ PC UART 通信

> 対象リポジトリ: `sosukigara/2026-Meister` | 作成日: 2026-08-12 | ステータス: 計画中

## 概要

ESP32 マイコンと PC を UART で接続し、**半分散型アーキテクチャ**でロボットを制御する。
リアルタイム性が要求されるモータ・サーボの PWM 制御は ESP32 が担い、ナビゲーション・画像認識・把持判断などの高度な処理は PC 上の ROS 2 が担う。

```
PC (ROS 2: Nav2 / 画像認識 / Web UI)
  │  UART 双方向通信
ESP32 (モータ・サーボ PWM 制御, センサ入力)
```

## 実装済み機能

- （未実装 — 計画中）

## 計画中機能

- [ ] PC(ROS 2) と ESP32 間の UART 双方向通信を確立する
- [ ] **指令送信（PC → ESP32）**: 駆動モータ速度・ステアリングサーボ角度・アームサーボ角度・グリッパー開閉指令を送る
- [ ] **フィードバック受信（ESP32 → PC）**: エンコーダ値・サーボ状態・センサ情報・エラー状態を返す
- [ ] リアルタイム制御（PWM 生成・サーボバス制御）を ESP32 側で完結させる
- [ ] 高度な判断（ナビ・認識・把持計画）を PC 側の ROS 2 で実行する

## 仕様化メモ

- **プロトコル推奨**: カスタムバイナリ（固定長フレーム + チェックサム）が決定的で堅牢。テキスト（JSON/改行区切り）はデバッグに有利だが非決定的。
- **ボーレート目安**: 115200 bps（最低ライン）。必要に応じて 921600 bps まで拡張。
- **配線**: PC ↔ ESP32 は USB-UART 変換 or ESP32 内蔵 USB。GND 共通必須。
- **電源分離**: サーボ・モータ駆動系とロジック系の電源分離を検討（ノイズ対策）。
